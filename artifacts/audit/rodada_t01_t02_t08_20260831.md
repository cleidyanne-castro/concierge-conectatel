# Evidência da rodada T01, T02 e T08 — 2026-08-31

Ambiente: conta AWS da equipe, região `us-east-1`, função
`concierge-conectatel-retrieve-kb`.

## T01 — resposta fundamentada

- `trace_id`: `t01-banca-20260831`
- pergunta: “Como consulto meu consumo de dados?”
- decisão retornada: `responder`
- melhor score: `0.9114037752151489`
- fonte retornada: `data/corpus/faq/faq_geral.md`
- status da fonte: `vigente`
- resultado: passou.

Consulta posterior ao CloudWatch encontrou o mesmo `trace_id`, a pergunta, a
decisão `responder` e o score no grupo
`/aws/lambda/concierge-conectatel-retrieve-kb`.

## T02 — ausência de fonte

- `trace_id`: `t02-banca-20260831`
- pergunta: “Qual será a previsão do tempo amanhã?”
- decisão retornada: `nao_sei`
- resultados retornados: nenhum
- limiar usado: `0.85`
- resultado: passou.

Consulta posterior ao CloudWatch encontrou o mesmo `trace_id`, a pergunta e a
decisão `nao_sei` no mesmo grupo de logs.

## T08 — verificações locais

Comando executado:

```bash
.venv/bin/python -m pytest tests/parte_01_dados \
  tests/parte_02_rag/test_vector_store.py \
  tests/parte_05_governanca tests/interface -q
```

Resultado: `27 passed in 0.66s`.

Também foi executado:

```bash
SAM_CLI_TELEMETRY=0 sam validate --template-file infra/template.yaml --region us-east-1
```

Resultado: template SAM válido.

## Limite desta evidência

Na Lambda de busca direta, o evento de auditoria registra `trace_id`, pergunta,
decisão e score. A resposta da função contém a fonte, mas fonte e guardrail
ainda precisam constar no evento consolidado do AgentCore/gateway para cumprir
integralmente o caso T06. Essa verificação permanece pendente da integração do
agente.
